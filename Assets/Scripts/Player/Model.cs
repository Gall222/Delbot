using UnityEngine;

namespace Player
{
    public class Model
    {
        public void Jump()
        {
            Debug.Log("Jump");
            // Логика прыжка — изменить состояние, поднять флаг, запустить физику и т.п.
        }

        public void Move()
        {
            Debug.Log("Move");
        }
    }
}