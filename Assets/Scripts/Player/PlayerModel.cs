
using UnityEngine;

namespace Player
{
    public class PlayerModel
    {
        public void Jump()
        {
            Debug.Log("Jump");
            // Логика прыжка — изменить состояние, поднять флаг, запустить физику и т.п.
        }
    }
}